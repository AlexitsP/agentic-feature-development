# Agent guidelines

**The canonical agent base for this repo is [`CLAUDE.md`](CLAUDE.md) — read it.** It is the single
source of truth (activation/governance, the binding guardrails, this repo's facts, parallel-work
rules, and conventions). This file exists only so non-Claude tools that look for `AGENTS.md` still
find the entry point; do not duplicate content here — keep `CLAUDE.md` the one place it lives.
